const analyzeText = (text) => {
    // Instead of calling an external API, return a static suggestion for now
    return "This is the academic writing suggestion";
};

module.exports = { analyzeText };
